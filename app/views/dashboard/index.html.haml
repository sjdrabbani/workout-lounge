.jumbotron
  %h1 Workout Lounge

  %p Share your workout with friends & family

.row
  .col-md-10
    %h2 List of members
    = will_paginate @athletes, renderer: BootstrapPagination::Rails, class: 'pull-left paginate'

.row
  - @athletes.each do |athlete|
    .col-md-10.well.member
      = athlete.full_name
      - if current_user && !current_user.follows_or_same?(athlete)
        = link_to 'Follow', friendships_path(friend_id: athlete.id),
          method: :post, class: 'btn btn-success pull-right'

